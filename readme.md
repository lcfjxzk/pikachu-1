## 开发
```
yarn global add parcel-bundler
parcel src/test.html
```
## 命令行
```
parcel build src/test.html --no-minify
rm -rf dist
parcel build src/test.html --no-minify
parcel build src/test.html --no-minify --public-url ./
git init
gst

创建文件.gitignore
并输入
/.cache/
/node_modules/
再次gst

git add .
git commit -v
git remote add origin git@github.com:lcfjxzk/pikachu-1.git
git push -u origin master
```